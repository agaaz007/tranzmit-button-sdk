/**
 * @tranzmit/exit-button-core
 * Core types and API client for Exit Button SDK
 */

// Export all types
export * from './types';

// Export API client
export { ExitButtonApiClient, createApiClient } from './api-client';
export type { ApiClientOptions } from './api-client';
