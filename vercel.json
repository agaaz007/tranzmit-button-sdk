{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "pnpm --filter @tranzmit/exit-button-core build && pnpm --filter @tranzmit/exit-button-embed build && pnpm --filter @tranzmit/exit-button-react build && mkdir -p public && echo '{}' > public/index.json",
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "includeFiles": "packages/embed/dist/**,packages/backend/src/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
